name: Deploy to VPS

on:
  push:
    branches:
      - main
    paths:
      - 'bible1/**'
      - 'bible1-members/**'
      - 'bible1-es/**'
      - 'bible1-it/**'
      - 'sc/**'
      - 'sc-es/**'
      - 'sc-it/**'
      - 'sc-pt/**'
      - 'py/**'
      - 'py-es/**'
      - 'py-fr/**'
      - 'smoothies/**'
      - 'chickens/**'
      - 'chickens-de/**'
      - 'Página modelo/**'
      - 'WW/**'
      - 'ww-de/**'
      - 'ww-fr/**'
      - 'ww-it/**'
      - 'ww-plans-fr/**'
      - 'ww-plans-it/**'
      - 'mw-de/**'
      - 'mw/**'
      - 'mw-pt/**'
      - 'bkp/**'
      - 'bkp-de/**'
      - 'bkp-es/**'
      - 'bkp-pt/**'
      - 'hx-fr/**'
      - 'hx-it/**'
      - 'beekeeping-members/**'
      - 'beekeeping-de-members/**'
      - 'beekeeping-fr-members/**'
      - 'beekeeping-it-members/**'
      - 'beekeeping-es-members/**'
      - 'beekeeping-pt-members/**'
      - 'ww-members/**'
      - 'ww-de-members/**'
      - 'metalon-members/**'
      - 'excel/**'
      - 'excel-plans-fr/**'
      - 'excel-plans-de/**'
      - 'excel-plans-es/**'
      - 'excel-plans-it/**'
      - 'excel-plans-pt/**'
      - 'excel-members/**'
      - 'excel-de-members/**'
      - 'excel-fr-members/**'
      - 'excel-es-members/**'
      - 'excel-it-members/**'
      - 'excel-pt-members/**'
      - 'off-grid/**'
      - 'off-grid-fr/**'
      - 'off-grid-de/**'
      - '.github/workflows/**'

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout Repository
        uses: actions/checkout@v4
        with:
          lfs: true

      - name: Prepare VPS Directories for Deployments
        uses: appleboy/ssh-action@v1.0.3
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          script: |
            sudo mkdir -p /var/www/storeinfocus.com/bible1
            sudo mkdir -p /var/www/storeinfocus.com/bible1-members
            sudo mkdir -p /var/www/storeinfocus.com/bible1-es
            sudo mkdir -p /var/www/storeinfocus.com/bible1-it
            sudo mkdir -p /var/www/storeinfocus.com/sc
            sudo mkdir -p /var/www/storeinfocus.com/sc-es
            sudo mkdir -p /var/www/storeinfocus.com/sc-it
            sudo mkdir -p /var/www/storeinfocus.com/sc-pt
            sudo mkdir -p /var/www/storeinfocus.com/py
            sudo mkdir -p /var/www/storeinfocus.com/py-es
            sudo mkdir -p /var/www/storeinfocus.com/py-fr
            sudo mkdir -p /var/www/storeinfocus.com/smoot
            sudo mkdir -p /var/www/storeinfocus.com/pagina-modelo
            sudo chown -R ${{ secrets.VPS_USERNAME }}:${{ secrets.VPS_USERNAME }} /var/www/storeinfocus.com || true
            sudo mkdir -p /var/www/skillvault.blog/ww
            sudo mkdir -p /var/www/skillvault.blog/chickens
            sudo mkdir -p /var/www/skillvault.blog/chickens-al
            sudo mkdir -p /var/www/skillvault.blog/ww-de
            sudo mkdir -p /var/www/skillvault.blog/ww-fr
            sudo mkdir -p /var/www/skillvault.blog/ww-it
            sudo mkdir -p /var/www/skillvault.blog/ww-plans-fr
            sudo mkdir -p /var/www/skillvault.blog/ww-plans-it
            sudo mkdir -p /var/www/skillvault.blog/mw-de
            sudo mkdir -p /var/www/skillvault.blog/mw
            sudo mkdir -p /var/www/skillvault.blog/mw-pt
            sudo mkdir -p /var/www/skillvault.blog/bkp
            sudo mkdir -p /var/www/skillvault.blog/bkp-de
            sudo mkdir -p /var/www/skillvault.blog/bkp-es
            sudo mkdir -p /var/www/skillvault.blog/bkp-pt
            sudo mkdir -p /var/www/skillvault.blog/hx-fr
            sudo mkdir -p /var/www/skillvault.blog/hx-it
            sudo mkdir -p /var/www/skillvault.blog/bkp-m
            sudo mkdir -p /var/www/skillvault.blog/bkp-de-m
            sudo mkdir -p /var/www/skillvault.blog/hx-fr-m
            sudo mkdir -p /var/www/skillvault.blog/hx-it-m
            sudo mkdir -p /var/www/skillvault.blog/hx-es-m
            sudo mkdir -p /var/www/skillvault.blog/hx-pt-m
            sudo mkdir -p /var/www/skillvault.blog/ww-m
            sudo mkdir -p /var/www/skillvault.blog/ww-de-m
            sudo mkdir -p /var/www/skillvault.blog/mw-m
            sudo mkdir -p /var/www/skillvault.blog/excel
            sudo mkdir -p /var/www/skillvault.blog/excel-plans-fr
            sudo mkdir -p /var/www/skillvault.blog/excel-plans-de
            sudo mkdir -p /var/www/skillvault.blog/excel-plans-es
            sudo mkdir -p /var/www/skillvault.blog/excel-plans-it
            sudo mkdir -p /var/www/skillvault.blog/excel-plans-pt
            sudo mkdir -p /var/www/skillvault.blog/excel-m
            sudo mkdir -p /var/www/skillvault.blog/excel-de-m
            sudo mkdir -p /var/www/skillvault.blog/excel-fr-m
            sudo mkdir -p /var/www/skillvault.blog/excel-es-m
            sudo mkdir -p /var/www/skillvault.blog/excel-it-m
            sudo mkdir -p /var/www/skillvault.blog/excel-pt-m
            sudo mkdir -p /var/www/skillvault.blog/off-grid
            sudo mkdir -p /var/www/skillvault.blog/off-grid-fr
            sudo mkdir -p /var/www/skillvault.blog/off-grid-de
            sudo chown -R ${{ secrets.VPS_USERNAME }}:${{ secrets.VPS_USERNAME }} /var/www/skillvault.blog || true

      # â”€â”€ BIBLE1 â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€

      - name: Deploy 'bible1' to storeinfocus.com/bible1
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "bible1/*"
          target: "/var/www/storeinfocus.com/bible1"
          strip_components: 1

      - name: Deploy 'bible1-members' to storeinfocus.com/bible1-members
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "bible1-members/*"
          target: "/var/www/storeinfocus.com/bible1-members"
          strip_components: 1

      - name: Deploy 'bible1-es' to storeinfocus.com/bible1-es
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "bible1-es/*"
          target: "/var/www/storeinfocus.com/bible1-es"
          strip_components: 1

      - name: Deploy 'bible1-it' to storeinfocus.com/bible1-it
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "bible1-it/*"
          target: "/var/www/storeinfocus.com/bible1-it"
          strip_components: 1

      # â”€â”€ SYSTEMATIC THEOLOGY â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€

      - name: Deploy 'sc' to storeinfocus.com/sc
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "sc/*"
          target: "/var/www/storeinfocus.com/sc"
          strip_components: 1

      - name: Deploy 'sc-es' to storeinfocus.com/sc-es
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "sc-es/*"
          target: "/var/www/storeinfocus.com/sc-es"
          strip_components: 1

      - name: Deploy 'sc-it' to storeinfocus.com/sc-it
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "sc-it/*"
          target: "/var/www/storeinfocus.com/sc-it"
          strip_components: 1

      - name: Deploy 'sc-pt' to storeinfocus.com/sc-pt
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "sc-pt/*"
          target: "/var/www/storeinfocus.com/sc-pt"
          strip_components: 1

      # â”€â”€ PROSPERITY â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€

      - name: Deploy 'py' to storeinfocus.com/py
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "py/*"
          target: "/var/www/storeinfocus.com/py"
          strip_components: 1

      - name: Deploy 'py-es' to storeinfocus.com/py-es
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "py-es/*"
          target: "/var/www/storeinfocus.com/py-es"
          strip_components: 1

      - name: Deploy 'py-fr' to storeinfocus.com/py-fr
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "py-fr/*"
          target: "/var/www/storeinfocus.com/py-fr"
          strip_components: 1

      - name: Deploy 'smoothies' to storeinfocus.com/smoot
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "smoothies/*"
          target: "/var/www/storeinfocus.com/smoot"
          strip_components: 1

      # â”€â”€ WOODWORKING â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€

      - name: Deploy 'Página modelo' to storeinfocus.com/pagina-modelo
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "Página modelo/*"
          target: "/var/www/storeinfocus.com/pagina-modelo"
          strip_components: 1

      - name: Deploy 'WW' to skillvault.blog/ww
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "WW/*"
          target: "/var/www/skillvault.blog/ww"
          strip_components: 1

      - name: Deploy 'chickens' to skillvault.blog/chickens
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "chickens/*"
          target: "/var/www/skillvault.blog/chickens"
          strip_components: 1

      - name: Deploy 'chickens-de' to skillvault.blog/chickens-al
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "chickens-de/*"
          target: "/var/www/skillvault.blog/chickens-al"
          strip_components: 1

      - name: Deploy 'ww-de' to skillvault.blog/ww-de
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "ww-de/*"
          target: "/var/www/skillvault.blog/ww-de"
          strip_components: 1

      - name: Deploy 'ww-fr' to skillvault.blog/ww-fr
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "ww-fr/*"
          target: "/var/www/skillvault.blog/ww-fr"
          strip_components: 1

      - name: Deploy 'ww-it' to skillvault.blog/ww-it
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "ww-it/*"
          target: "/var/www/skillvault.blog/ww-it"
          strip_components: 1

      - name: Deploy 'ww-plans-fr' to skillvault.blog/ww-plans-fr
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "ww-plans-fr/*"
          target: "/var/www/skillvault.blog/ww-plans-fr"
          strip_components: 1

      - name: Deploy 'ww-plans-it' to skillvault.blog/ww-plans-it
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "ww-plans-it/*"
          target: "/var/www/skillvault.blog/ww-plans-it"
          strip_components: 1

      - name: Deploy 'mw-de' to skillvault.blog/mw-de
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "mw-de/*"
          target: "/var/www/skillvault.blog/mw-de"
          strip_components: 1

      - name: Deploy 'mw' to skillvault.blog/mw
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "mw/*"
          target: "/var/www/skillvault.blog/mw"
          strip_components: 1

      - name: Deploy 'mw-pt' to skillvault.blog/mw-pt
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "mw-pt/*"
          target: "/var/www/skillvault.blog/mw-pt"
          strip_components: 1

      - name: Deploy 'bkp' to skillvault.blog/bkp
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "bkp/*"
          target: "/var/www/skillvault.blog/bkp"
          strip_components: 1

      - name: Deploy 'bkp-de' to skillvault.blog/bkp-de
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "bkp-de/*"
          target: "/var/www/skillvault.blog/bkp-de"
          strip_components: 1

      - name: Deploy 'bkp-es' to skillvault.blog/bkp-es
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "bkp-es/*"
          target: "/var/www/skillvault.blog/bkp-es"
          strip_components: 1

      - name: Deploy 'bkp-pt' to skillvault.blog/bkp-pt
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "bkp-pt/*"
          target: "/var/www/skillvault.blog/bkp-pt"
          strip_components: 1

      - name: Deploy 'hx-fr' to skillvault.blog/hx-fr
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "hx-fr/*"
          target: "/var/www/skillvault.blog/hx-fr"
          strip_components: 1

      - name: Deploy 'hx-it' to skillvault.blog/hx-it
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "hx-it/*"
          target: "/var/www/skillvault.blog/hx-it"
          strip_components: 1

      # â”€â”€ BEEKEEPING MEMBERS AREAS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€

      - name: Deploy 'beekeeping-members' to skillvault.blog/bkp-m
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "beekeeping-members/*"
          target: "/var/www/skillvault.blog/bkp-m"
          strip_components: 1

      - name: Deploy 'beekeeping-de-members' to skillvault.blog/bkp-de-m
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "beekeeping-de-members/*"
          target: "/var/www/skillvault.blog/bkp-de-m"
          strip_components: 1

      - name: Deploy 'beekeeping-fr-members' to skillvault.blog/hx-fr-m
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "beekeeping-fr-members/*"
          target: "/var/www/skillvault.blog/hx-fr-m"
          strip_components: 1

      - name: Deploy 'beekeeping-it-members' to skillvault.blog/hx-it-m
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "beekeeping-it-members/*"
          target: "/var/www/skillvault.blog/hx-it-m"
          strip_components: 1

      - name: Deploy 'beekeeping-es-members' to skillvault.blog/hx-es-m
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "beekeeping-es-members/*"
          target: "/var/www/skillvault.blog/hx-es-m"
          strip_components: 1

      - name: Deploy 'beekeeping-pt-members' to skillvault.blog/hx-pt-m
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "beekeeping-pt-members/*"
          target: "/var/www/skillvault.blog/hx-pt-m"
          strip_components: 1

      # â”€â”€ WOODWORKING MEMBERS AREAS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€

      - name: Deploy 'ww-members' to skillvault.blog/ww-m
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "ww-members/*"
          target: "/var/www/skillvault.blog/ww-m"
          strip_components: 1

      - name: Deploy 'ww-de-members' to skillvault.blog/ww-de-m
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "ww-de-members/*"
          target: "/var/www/skillvault.blog/ww-de-m"
          strip_components: 1

      # â”€â”€ METALWORKING MEMBERS AREAS â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€

      - name: Deploy 'metalon-members' to skillvault.blog/mw-m
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "metalon-members/*"
          target: "/var/www/skillvault.blog/mw-m"
          strip_components: 1

      # â”€â”€ EXCEL â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€â”€

      - name: Deploy 'excel' to skillvault.blog/excel
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "excel/*"
          target: "/var/www/skillvault.blog/excel"
          strip_components: 1

      - name: Deploy 'excel-plans-fr' to skillvault.blog/excel-plans-fr
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "excel-plans-fr/*"
          target: "/var/www/skillvault.blog/excel-plans-fr"
          strip_components: 1

      - name: Deploy 'excel-plans-de' to skillvault.blog/excel-plans-de
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "excel-plans-de/*"
          target: "/var/www/skillvault.blog/excel-plans-de"
          strip_components: 1

      - name: Deploy 'excel-plans-es' to skillvault.blog/excel-plans-es
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "excel-plans-es/*"
          target: "/var/www/skillvault.blog/excel-plans-es"
          strip_components: 1

      - name: Deploy 'excel-plans-it' to skillvault.blog/excel-plans-it
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "excel-plans-it/*"
          target: "/var/www/skillvault.blog/excel-plans-it"
          strip_components: 1

      - name: Deploy 'excel-plans-pt' to skillvault.blog/excel-plans-pt
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "excel-plans-pt/*"
          target: "/var/www/skillvault.blog/excel-plans-pt"
          strip_components: 1

      - name: Deploy 'excel-members' to skillvault.blog/excel-m
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "excel-members/*"
          target: "/var/www/skillvault.blog/excel-m"
          strip_components: 1

      - name: Deploy 'excel-de-members' to skillvault.blog/excel-de-m
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "excel-de-members/*"
          target: "/var/www/skillvault.blog/excel-de-m"
          strip_components: 1

      - name: Deploy 'excel-fr-members' to skillvault.blog/excel-fr-m
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "excel-fr-members/*"
          target: "/var/www/skillvault.blog/excel-fr-m"
          strip_components: 1

      - name: Deploy 'excel-es-members' to skillvault.blog/excel-es-m
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "excel-es-members/*"
          target: "/var/www/skillvault.blog/excel-es-m"
          strip_components: 1

      - name: Deploy 'excel-it-members' to skillvault.blog/excel-it-m
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "excel-it-members/*"
          target: "/var/www/skillvault.blog/excel-it-m"
          strip_components: 1

      - name: Deploy 'excel-pt-members' to skillvault.blog/excel-pt-m
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "excel-pt-members/*"
          target: "/var/www/skillvault.blog/excel-pt-m"
          strip_components: 1

      - name: Deploy 'off-grid' to skillvault.blog/off-grid
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "off-grid/*"
          target: "/var/www/skillvault.blog/off-grid"
          strip_components: 1

      - name: Deploy 'off-grid-fr' to skillvault.blog/off-grid-fr
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "off-grid-fr/*"
          target: "/var/www/skillvault.blog/off-grid-fr"
          strip_components: 1

      - name: Deploy 'off-grid-de' to skillvault.blog/off-grid-de
        uses: appleboy/scp-action@v0.1.7
        with:
          host: ${{ secrets.VPS_HOST }}
          username: ${{ secrets.VPS_USERNAME }}
          key: ${{ secrets.VPS_SECRET }}
          source: "off-grid-de/*"
          target: "/var/www/skillvault.blog/off-grid-de"
          strip_components: 1
